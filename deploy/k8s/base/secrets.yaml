# NOTE: This file contains placeholder secrets for base configuration.
# In production, use External Secrets Operator or Vault integration.
# Never commit real secrets to version control.

apiVersion: v1
kind: Secret
metadata:
  name: pake-secrets
  namespace: pake-system
  labels:
    app.kubernetes.io/name: pake-secrets
    app.kubernetes.io/component: secrets
  annotations:
    config.kubernetes.io/warning: 'This contains placeholder secrets. Use External Secrets Operator in production.'
type: Opaque
stringData:
  # Database Secrets (Base64 encoded in actual use)
  DATABASE_PASSWORD: 'changeme-database-REDACTED_SECRET'

  # Redis Secrets
  REDIS_PASSWORD: 'changeme-redis-REDACTED_SECRET'

  # JWT Secrets
  JWT_SECRET_KEY: 'changeme-jwt-secret-key-make-this-long-and-random'

  # API Keys (Placeholders - retrieve from Vault in production)
  FIRECRAWL_API_KEY: 'placeholder-firecrawl-api-key'
  OPENAI_API_KEY: 'placeholder-openai-api-key'
  ANTHROPIC_API_KEY: 'placeholder-anthropic-api-key'

  # Application Secrets
  APP_SECRET_KEY: 'changeme-app-secret-key'
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: pake-system
  labels:
    app.kubernetes.io/name: postgres-secret
    app.kubernetes.io/component: database
type: Opaque
stringData:
  POSTGRES_PASSWORD: 'changeme-database-REDACTED_SECRET'
  POSTGRES_REPLICATION_PASSWORD: 'changeme-replication-REDACTED_SECRET'
---
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: pake-system
  labels:
    app.kubernetes.io/name: redis-secret
    app.kubernetes.io/component: cache
type: Opaque
stringData:
  REDIS_PASSWORD: 'changeme-redis-REDACTED_SECRET'
