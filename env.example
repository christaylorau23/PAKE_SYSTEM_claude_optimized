# PAKE System Environment Configuration
# CRITICAL: Copy this file to .env and configure with proper secrets
# NEVER commit the actual .env file to version control

# =============================================================================
# AUTHENTICATION & SECURITY (REQUIRED)
# =============================================================================

# JWT Secret - MUST be a strong, randomly generated secret (minimum 32 characters)
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production-minimum-32-chars

# Database Password - MUST be a strong REDACTED_SECRET for PostgreSQL
DB_PASSWORD=your-super-secure-database-REDACTED_SECRET-change-this-in-production

# Redis Password - MUST be a strong REDACTED_SECRET for Redis
REDIS_PASSWORD=your-super-secure-redis-REDACTED_SECRET-change-this-in-production

# =============================================================================
# API KEYS (REQUIRED FOR PRODUCTION)
# =============================================================================

# PAKE System API Key - Internal system authentication
PAKE_API_KEY=your-super-secure-pake-api-key-change-this-in-production

# Anthropic Claude API Key - For AI provider integration
ANTHROPIC_API_KEY=your-anthropic-claude-api-key-from-anthropic-console

# Google Gemini API Key - For AI provider integration  
GEMINI_API_KEY=your-google-gemini-api-key-from-google-console

# =============================================================================
# VIDEO GENERATION SERVICES (OPTIONAL)
# =============================================================================

# D-ID API Key - For video generation (optional)
DID_API_KEY=your-did-api-key-if-using-video-generation

# HeyGen API Key - For video generation (optional)
HEYGEN_API_KEY=your-heygen-api-key-if-using-video-generation

# =============================================================================
# WEBHOOK SECURITY (REQUIRED IF USING WEBHOOKS)
# =============================================================================

# Webhook Secret - For webhook signature verification
WEBHOOK_SECRET=your-super-secure-webhook-secret-for-signature-verification

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

DB_HOST=localhost
DB_PORT=5432
DB_NAME=pake_system
DB_USER=pake_user

# =============================================================================
# REDIS CONFIGURATION  
# =============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

NODE_ENV=development
PORT=3001
LOG_LEVEL=info

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. ALL secrets must be at least 16 characters long
# 2. Use a REDACTED_SECRET manager to generate strong, random secrets
# 3. NEVER use default or weak REDACTED_SECRETs in production
# 4. Rotate secrets regularly (recommended: every 90 days)
# 5. Use different secrets for different environments (dev/staging/prod)
# 6. Store production secrets in a secure secrets manager (HashiCorp Vault, AWS Secrets Manager, etc.)
