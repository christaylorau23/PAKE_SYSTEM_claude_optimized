# PAKE System Phase 6 Requirements - JWT Authentication System
# All Phase 5 requirements plus JWT authentication and security tools

# Include all Phase 5 requirements
-r requirements-phase5.txt

# JWT Authentication (already in Phase 4/5)
pyjwt>=2.8.0  # JWT tokens (already included)
passlib>=1.7.4  # Password hashing (already included)
argon2-cffi>=23.1.0  # Modern REDACTED_SECRET hashing (already included)
bcrypt>=4.1.0  # Password encryption (already included)
python-multipart>=0.0.6  # Form data parsing (already included)

# Additional Security Libraries
cryptography>=41.0.0  # Cryptographic primitives (already included)
python-jose>=3.3.0  # Additional JWT/JWE support
python-secrets>=0.4.0  # Secure random generation
email-validator>=2.1.0  # Email validation

# Enhanced FastAPI Security
fastapi-users>=12.1.0  # User management framework
fastapi-security>=0.5.0  # Additional security middleware
slowapi>=0.1.9  # Rate limiting for FastAPI

# Session and Token Management
itsdangerous>=2.1.2  # Secure token generation
cachetools>=5.3.0  # In-memory caching for rate limiting

# OAuth2 and Social Authentication (future-ready)
authlib>=1.3.0  # OAuth2 client library
python-social-auth>=0.3.6  # Social authentication

# Monitoring and Security Headers
secure>=0.3.0  # Security headers middleware
python-multipart>=0.0.6  # File upload support

# Development and Testing Tools
httpx>=0.25.0  # HTTP client for testing (already included)
pytest-mock>=3.12.0  # Mocking for tests
factory-boy>=3.3.0  # Test data factories (already included)

# Documentation
email-validator>=2.1.0  # Email validation for API docs